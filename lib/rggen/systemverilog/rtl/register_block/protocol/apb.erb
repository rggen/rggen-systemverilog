rggen_apb_adapter #(
  .ADDRESS_WIDTH  (<%= local_address_width %>),
  .BUS_WIDTH      (<%= bus_width %>),
  .REGISTERS      (<%= total_registers %>)
) u_adapter (
  .i_clk        (<%= clock %>),
  .i_rst_n      (<%= reset %>),
  .apb_if       (<%= apb_if %>),
  .register_if  (<%= register_if %>)
);
